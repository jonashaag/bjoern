
        TODO/BUGS
    =================

* There's a memory leak after ~ 20,000 requests (two `make ab` runs)
* wsgi_getheaders: get rid of buf_write (but memcpy is bad for that small strings)
* sendfile
* body -> tempfile for large requests
